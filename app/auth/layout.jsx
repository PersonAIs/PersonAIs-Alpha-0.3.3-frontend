export const metadata = {
  title: "Sign In",
  description:
    "Log in to PersonAIs Alpha 0.4.1, or create an account to build a digital twin.",
};

export default function AuthLayout({ children }) {
  return children;
}
